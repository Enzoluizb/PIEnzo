const { StyleSheet } = require("react-native");

export const style = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    h1 : {
        fontSize: "30px",
        fontWeight: "bold"
    },
});